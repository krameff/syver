---
service:
  webservice:
    enabled: false
    running: false
{{ if .Env.OS | regexMatch "centos[7]|rockylinux[9]|almalinux10" }}
  httpd:
{{else}}
  apache2:
{{end}}
{{ if .Env.OS | regexMatch "trusty" }}
    enabled: false
{{else}}
    enabled: true
{{end}}
    running: true
{{ if .Env.OS | regexMatch "alpine3" }}
    runlevels: ["sysinit"]
{{ else if .Env.OS | regexMatch "rockylinux9|almalinux10|jammy|bullseye" }}
    runlevels: []
{{end}}
  skippable:
    enabled: true
    running: true
    skip: true
